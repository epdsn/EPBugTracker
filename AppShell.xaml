<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="EPBugTracker.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:EPBugTracker"
    Title="EPBugTracker"
    Shell.TabBarIsVisible="False">

    <!-- Title + route links on one row, menu placed immediately after title -->
    <Shell.TitleView>
        <Grid Padding="20, 0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <!-- title width based on content -->
                <ColumnDefinition Width="Auto" />
                <!-- menu width based on content and placed next to title -->
                <ColumnDefinition Width="Auto" />
                <!-- filler to take remaining space -->
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- App title -->
            <Label Text="EP Bug Tracker"
                   FontSize="20"
                   VerticalOptions="Center"
                   HorizontalOptions="Start"
                   Grid.Column="0" />

            <!-- Route links (styled using RouteLinkBrush) placed immediately after title -->
            <HorizontalStackLayout 
                Grid.Column="1" 
                Spacing="12" 
                VerticalOptions="Center" 
                HorizontalOptions="Start">
                <Button x:Name="HomeLinkBtn"
                        Text="Home"
                        Clicked="OnNavigateHome"
                        BackgroundColor="Transparent"
                        TextColor="{StaticResource RouteLink}" />

                <Button x:Name="UsersLinkBtn"
                        Text="Users"
                        Clicked="OnNavigateUsers"
                        BackgroundColor="Transparent"
                        TextColor="{StaticResource RouteLink}" />
            </HorizontalStackLayout>
        </Grid>
    </Shell.TitleView>

    <TabBar>
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
        <ShellContent
            Title="Users"
            ContentTemplate="{DataTemplate local:UsersPage}"
            Route="UsersPage" />
    </TabBar>

</Shell>
